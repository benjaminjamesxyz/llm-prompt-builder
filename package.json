{
  "name": "llm-prompt-builder",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "bun@1.3.5",
  "scripts": {
    "dev": "rsbuild dev",
    "build": "rsbuild build",
    "preview": "rsbuild preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:ci": "vitest run",
    "test:coverage": "vitest run --coverage",
    "deploy:prod": "bun run build && bunx wrangler pages deploy dist --project-name=llm-prompt-builder",
    "deploy:staging": "bun run build && bunx wrangler pages deploy dist --project-name=llm-prompt-builder"
  },
  "dependencies": {
    "dompurify": "^3.3.1",
    "js-yaml": "^4.1.1",
    "preact": "^10.28.1",
    "prismjs": "^1.30.0"
  },
  "devDependencies": {
    "@rsbuild/core": "^1.7.1",
    "@rsbuild/plugin-preact": "^1.7.0",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^25.0.3",
    "@types/prismjs": "^1.26.5",
    "@testing-library/preact": "^3.2.4",
    "@testing-library/jest-dom": "^6.9.1",
    "@vitest/coverage-v8": "^4.0.16",
    "@tailwindcss/postcss": "^4.1.18",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.18",
    "typescript": "^5.9.3",
    "vitest": "^4.0.16",
    "jsdom": "^27.4.0"
  }
}
